# ranger rifle config
# rnvimr intercepts rules containing $EDITOR and opens the selected file
# through Neovim RPC. Standalone ranger still uses the configured editor.
file, label editor = ${VISUAL:-$EDITOR} -- "$@"
